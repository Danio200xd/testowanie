---
- name: Pokaz komunikatu na zdalnym serwerze
  hosts: all
  become: true  # lub true, jeśli potrzebujesz roota
  tasks:
    - name: Wyswietl komunikat
      ansible.builtin.shell: echo "Witaj ze świata Ansible!" | wall
      register: wynik

    - name: Pokaz wynik na lokalnym ekranie
      ansible.builtin.debug:
        var: wynik.stdout
